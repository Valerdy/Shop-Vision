# API Configuration
VITE_API_URL=http://localhost:3000/api/v1

# Mode de fonctionnement
# production: Utilise uniquement l'API backend (échoue si backend indisponible)
# development: Utilise l'API backend avec fallback sur données statiques si indisponible
# offline: Utilise uniquement les données statiques
VITE_APP_MODE=development

# Activer les logs de debug
VITE_DEBUG=true
